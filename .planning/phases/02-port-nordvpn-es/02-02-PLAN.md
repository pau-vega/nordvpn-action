---
phase: "02-port-nordvpn-es"
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - actions/nordvpn-es/disconnect/action.yml
  - actions/nordvpn-es/scripts/disconnect.sh
autonomous: true
requirements: [NVES-10, NVES-11]
user_setup: []
must_haves:
  truths:
    - "A caller running the sibling disconnect step with `if: always()` sees openvpn killed and auth file removed"
    - "The disconnect step never fails the job (best-effort cleanup)"
    - "No secret residue remains on the runner after disconnect runs"
    - "Even if the connect step failed mid-run, disconnect still runs (if: always()) and cleans up"
  artifacts:
    - path: "actions/nordvpn-es/disconnect/action.yml"
      provides: "Sibling composite sub-action for teardown, calls disconnect.sh"
      contains: "using: composite"
    - path: "actions/nordvpn-es/scripts/disconnect.sh"
      provides: "Best-effort cleanup: kill openvpn, remove auth file"
      contains: "set -u"
  key_links:
    - from: "actions/nordvpn-es/disconnect/action.yml"
      to: "scripts/disconnect.sh"
      via: "runs.steps[0].run"
      pattern: "scripts/disconnect.sh"
    - from: "scripts/disconnect.sh"
      to: "$RUNNER_TEMP/nordvpn-auth.txt"
      via: "rm -f"
      pattern: "rm -f.*nordvpn-auth.txt"
    - from: "action.yml (main action)"
      to: "disconnect/action.yml"
      via: "consumer workflow calls as sibling step"
      pattern: "uses:.*nordvpn-es/disconnect"
---

<objective>
Port the `disconnect/` sub-action from Tutellus PR #159: `disconnect/action.yml` and `scripts/disconnect.sh`. The disconnect action is a best-effort teardown that kills openvpn and removes the auth file. It must never fail the job (uses `set -u` only, NOT `-e`). Consumers invoke it as a sibling step with `if: always()`.

Purpose: Fulfill the composite-action `post:` gap (GitHub Discussion #26743) with a reliable cleanup action that leaves no secret residue.

Output: `actions/nordvpn-es/disconnect/` tree ready for consumer use.
</objective>

<execution_context>
@/Users/pauvelascogarrofe/.config/opencode/get-shit-done/workflows/execute-plan.md
@/Users/pauvelascogarrofe/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@/Users/pauvelascogarrofe/Documents/nordvpn-action/.planning/PROJECT.md
@/Users/pauvelascogarrofe/Documents/nordvpn-action/.planning/ROADMAP.md
@/Users/pauvelascogarrofe/Documents/nordvpn-action/.planning/REQUIREMENTS.md
@/Users/pauvelascogarrofe/Documents/nordvpn-action/.planning/phases/02-port-nordvpn-es/02-CONTEXT.md
@/Users/pauvelascogarrofe/Documents/nordvpn-action/AGENTS.md

# Source files (fetched from Tutellus/tutellus-frontend-utils PR #159)
# disconnect/action.yml: calls ../scripts/disconnect.sh (sibling path)
# scripts/disconnect.sh: best-effort kill openvpn, remove auth file, set -u only
</context>

<tasks>

<task type="auto">
  <name>Task 1: Port disconnect/action.yml</name>
  <files>actions/nordvpn-es/disconnect/action.yml</files>
  <read_first>
    - /Users/pauvelascogarrofe/Documents/nordvpn-action/.planning/phases/02-port-nordvpn-es/02-CONTEXT.md (D-12: keep relative path ../scripts/disconnect.sh in disconnect/action.yml)
    - Source PR: https://github.com/Tutellus/tutellus-frontend-utils/pull/159 (file: actions/nordvpn-es/disconnect/action.yml)
  </read_first>
  <action>
    Create `actions/nordvpn-es/disconnect/action.yml` by copying from source PR #159, updating repo references:
    - Keep `name: "NordVPN Spanish Egress — Disconnect"` (or update to match pau-vega/nordvpn-actions naming)
    - Keep `description: "Tear down the NordVPN tunnel and remove the runtime auth file. Intended as an if: always() sibling step..."`
    - Keep `using: "composite"` with single step calling `"${{ github.action_path }}/../scripts/disconnect.sh"` (D-12: relative path to sibling scripts/ directory)
    - Update any Tutellus references to pau-vega/nordvpn-actions
    
    The disconnect action MUST:
    - Be a composite sub-action (not a separate workflow)
    - Call `disconnect.sh` via relative path `../scripts/disconnect.sh` (scripts/ is sibling to disconnect/)
    - Document that it never fails the job (best-effort)
    - Mention `if: always()` requirement in description (NVES-10)
  </action>
  <verify>
    <automated>actionlint actions/nordvpn-es/disconnect/action.yml 2>&1 | head -20</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "using: composite" actions/nordvpn-es/disconnect/action.yml` returns 1
    - `grep -c "../scripts/disconnect.sh" actions/nordvpn-es/disconnect/action.yml` returns 1 (D-12: relative path)
    - `grep -c "if: always" actions/nordvpn-es/disconnect/action.yml` returns 0 (the action doesn't contain if: always — that's in the consumer workflow; description just mentions it)
    - `grep -c "pau-vega/nordvpn-actions" actions/nordvpn-es/disconnect/action.yml` returns at least 1 OR `grep -c "Tutellus/tutellus-frontend-utils" actions/nordvpn-es/disconnect/action.yml` returns 0 (repo ref updated)
    - actionlint passes with no errors
  </acceptance_criteria>
  <done>disconnect/action.yml is a composite action calling ../scripts/disconnect.sh, repo refs updated, passes actionlint</done>
</task>

<task type="auto">
  <name>Task 2: Port disconnect.sh script</name>
  <files>actions/nordvpn-es/scripts/disconnect.sh</files>
  <read_first>
    - /Users/pauvelascogarrofe/Documents/nordvpn-action/.planning/phases/02-port-nordvpn-es/02-CONTEXT.md (D-10: disconnect.sh uses set -u only, NOT -e; D-11: keep sudo usage)
    - /Users/pauvelascogarrofe/Documents/nordvpn-action/AGENTS.md (Security: never set -x, no credential echo)
    - Source PR: https://github.com/Tutellus/tutellus-frontend-utils/pull/159 (file: actions/nordvpn-es/scripts/disconnect.sh)
  </read_first>
  <action>
    Create `actions/nordvpn-es/scripts/disconnect.sh` by copying from source PR #159, updating repo references:
    - Keep `#!/usr/bin/env bash` shebang
    - Keep `set -u` ONLY (NOT `-e`, NOT `-o pipefail`) — best-effort cleanup must not fail the job (D-10, NVES-10)
    - NEVER `set -x` (NVES-11, AGENTS.md Security)
    
    Cleanup sequence (NVES-10):
    1. Read PID from `$RUNNER_TEMP/openvpn.pid` (or `/tmp/openvpn.pid` fallback)
    2. If PID exists and process running: `sudo kill -TERM`, wait up to 5s with check, then `sudo kill -KILL` if still alive
    3. `rm -f "$RUNNER_TEMP/nordvpn-auth.txt"` — auth file MUST be removed (NVES-10: no secret residue)
    4. `sudo pkill -TERM -x openvpn` as belt-and-braces (catches daemons without PID file)
    5. Non-fatal diagnostic: check if `ip route` still shows `dev tun0` after kill, warn if so
    6. Always `exit 0` — must not fail the job (NVES-10)
    
    - Keep `sudo` usage for kill/openvpn commands (D-11)
    - Keep `set -u` for variable existence checking
    - Update any Tutellus references to pau-vega/nordvpn-actions
  </action>
  <verify>
    <automated>shellcheck actions/nordvpn-es/scripts/disconnect.sh 2>&1 | head -20</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "^set -u$" actions/nordvpn-es/scripts/disconnect.sh` returns 1 (D-10: set -u ONLY)
    - `grep -c "set -e" actions/nordvpn-es/scripts/disconnect.sh` returns 0 (D-10: NOT -e)
    - `grep -c "set -o pipefail" actions/nordvpn-es/scripts/disconnect.sh` returns 0 (D-10: NOT pipefail)
    - `grep -c "set -x" actions/nordvpn-es/scripts/disconnect.sh` returns 0 (NVES-11: never set -x)
    - `grep -c "rm -f.*nordvpn-auth.txt" actions/nordvpn-es/scripts/disconnect.sh` returns 1 (NVES-10: auth file removed)
    - `grep -c "kill.*openvpn" actions/nordvpn-es/scripts/disconnect.sh` returns at least 1 (NVES-10: kill openvpn)
    - `grep -c "exit 0" actions/nordvpn-es/scripts/disconnect.sh` returns at least 1 (NVES-10: never fails job)
    - `grep -c "Tutellus/tutellus-frontend-utils" actions/nordvpn-es/scripts/disconnect.sh` returns 0 (repo ref updated)
    - shellcheck passes with no errors
  </acceptance_criteria>
  <done>disconnect.sh uses set -u only (not -e), kills openvpn, removes auth file, exits 0, no set -x, passes shellcheck</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Consumer workflow → disconnect action | Trusted: runs as `if: always()` sibling step |
| disconnect.sh → openvpn process | Local: signal-based termination |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-10 | Spoofing | N/A | accept | No credentials handled in disconnect |
| T-02-11 | Tampering | N/A | accept | No config files modified |
| T-02-12 | Repudiation | N/A | accept | GitHub Actions provides audit log |
| T-02-13 | Information Disclosure | openvpn.log or auth file | mitigate | `rm -f` auth file (NVES-10); disconnect.sh does not echo secrets; `set -x` banned (NVES-11) |
| T-02-14 | Denial of Service | N/A | accept | Disconnect is best-effort; `exit 0` ensures it never fails job |
| T-02-15 | Elevation of Privilege | `sudo kill` | accept | Ubuntu runner requires sudo for process management; no extra escalation |
| T-02-16 | Secret Residue | Auth file lingers after disconnect | mitigate | `rm -f "$RUNNER_TEMP/nordvpn-auth.txt"` (NVES-10); belt-and-braces pkill catches stray processes |
</threat_model>

<verification>
- [ ] `actionlint actions/nordvpn-es/disconnect/action.yml` passes with no errors
- [ ] `shellcheck actions/nordvpn-es/scripts/disconnect.sh` passes with no errors
- [ ] disconnect.sh uses `set -u` ONLY (not `-e`, not `pipefail`) per D-10
- [ ] Auth file removed, openvpn killed, `exit 0` present
- [ ] No `Tutellus/tutellus-frontend-utils` references remain
</verification>

<success_criteria>
- `actions/nordvpn-es/disconnect/action.yml` exists, calls `../scripts/disconnect.sh`, passes actionlint
- `actions/nordvpn-es/scripts/disconnect.sh` uses `set -u` only, kills openvpn, removes auth file, exits 0
- No `set -x`, no Tutellus references, passes shellcheck
- Disconnect action is best-effort: never fails the job (NVES-10, NVES-11)
</success_criteria>

<output>
After completion, create `.planning/phases/02-port-nordvpn-es/02-02-SUMMARY.md`
</output>
