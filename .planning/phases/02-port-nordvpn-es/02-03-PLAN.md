---
phase: "02-port-nordvpn-es"
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - actions/nordvpn-es/README.md
autonomous: true
requirements: [NVES-12, NVES-13]
user_setup: []
must_haves:
  truths:
    - "A consumer reading actions/nordvpn-es/README.md sees Inputs, Outputs, Usage with mandatory if: always() disconnect step"
    - "README documents three pin forms (SHA first, exact tag, floating major) with when-to-use guidance"
    - "README never recommends @main; floating-major mutability is explicitly documented"
    - "Credential Rotation section explains step-by-step how to update NORDVPN_SERVICE_USERNAME/PASSWORD in Preview environment"
    - "Troubleshooting section lists AUTH_FAILED, country mismatch, fork PR skip with fixes"
  artifacts:
    - path: "actions/nordvpn-es/README.md"
      provides: "Consumer-facing documentation: inputs, outputs, usage, versioning, troubleshooting"
      contains: "## Usage"
  key_links:
    - from: "actions/nordvpn-es/README.md"
      to: "action.yml"
      via: "uses: reference in Usage section"
      pattern: "uses:.*nordvpn-es"
    - from: "actions/nordvpn-es/README.md"
      to: "disconnect/action.yml"
      via: "disconnect step in Usage example"
      pattern: "disconnect.*@"
---

<objective>
Port and rewrite `actions/nordvpn-es/README.md` from Tutellus PR #159. Update all repo references from `Tutellus/tutellus-frontend-utils` to `pau-vega/nordvpn-actions`. Ensure the README documents: Inputs, Outputs, Usage (with mandatory `if: always()` disconnect), Versioning (three pin forms, SHA first), Credential Rotation (Preview environment), and Troubleshooting (AUTH_FAILED, country mismatch, fork PR skip).

Purpose: Provide consumer-facing documentation that teaches correct usage patterns and prevents common misconfigurations (NVES-12, NVES-13).

Output: Complete README.md for the nordvpn-es action.
</objective>

<execution_context>
@/Users/pauvelascogarrofe/.config/opencode/get-shit-done/workflows/execute-plan.md
@/Users/pauvelascogarrofe/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@/Users/pauvelascogarrofe/Documents/nordvpn-action/.planning/PROJECT.md
@/Users/pauvelascogarrofe/Documents/nordvpn-action/.planning/ROADMAP.md
@/Users/pauvelascogarrofe/Documents/nordvpn-action/.planning/REQUIREMENTS.md
@/Users/pauvelascogarrofe/Documents/nordvpn-action/.planning/phases/02-port-nordvpn-es/02-CONTEXT.md
@/Users/pauvelascogarrofe/Documents/nordvpn-action/AGENTS.md

# Source README from Tutellus PR #159 (fetched earlier, full content in context)
# Key sections to preserve: NordVPN Spanish Egress, Inputs table, Outputs table, Usage (with if: always()), Internal Steps, Credential Rotation, Versioning (3 pin forms), Troubleshooting
</context>

<tasks>

<task type="auto">
  <name>Task 1: Port and rewrite README.md for pau-vega/nordvpn-actions</name>
  <files>actions/nordvpn-es/README.md</files>
  <read_first>
    - /Users/pauvelascogarrofe/Documents/nordvpn-action/.planning/phases/02-port-nordvpn-es/02-CONTEXT.md (D-03: rewrite README for pau-vega/nordvpn-actions, update all repo references)
    - Source PR: https://github.com/Tutellus/tutellus-frontend-utils/pull/159 (file: actions/nordvpn-es/README.md)
    - /Users/pauvelascogarrofe/Documents/nordvpn-action/AGENTS.md (Code Style: uses: line format with SHA + # vX.Y.Z)
  </read_first>
  <action>
    Create `actions/nordvpn-es/README.md` by rewriting from source PR #159:
    
    **Header + Description** (NVES-12):
    - Title: `# NordVPN Spanish Egress (composite action)`
    - Update description: "Connects the current GitHub Actions runner to a NordVPN Spanish exit node, verifies `country=ES` against two independent geo providers, and exposes a diagnostics bundle as step outputs. Intended to be consumed from any repo via `uses:`."
    - Update teardown constraint section: "Composite actions do not support `post:` ([GitHub Community Discussion #26743](https://github.com/orgs/community/discussions/26743)). The caller workflow MUST invoke the `/disconnect` sub-action as a sibling step with `if: always()`."
    
    **Inputs table** (NVES-12):
    - `username` (required): "NordVPN service username. In consumer repos, sourced from a `Preview` environment secret like `NORDVPN_SERVICE_USERNAME`."
    - `password` (required): "NordVPN service password. In consumer repos, sourced from a `Preview` environment secret like `NORDVPN_SERVICE_PASSWORD`."
    - Add note: "NordVPN **service** credentials are required — NordVPN account email/password does NOT work with manual OpenVPN. Generate service credentials from the NordVPN web dashboard."
    
    **Outputs table** (NVES-12, NVES-02):
    - `exit-ip`: "Public IPv4 after tunnel up (NordVPN Spanish gateway)."
    - `country`: "ISO-2 country code of exit IP; guaranteed `ES` on action success."
    - `asn`: "ASN/ISP of the exit gateway (string)."
    - `tun0-state`: "Human-readable tunnel state: `up` or `down-or-missing`."
    - `default-route`: "Active IPv4 default route after connect (string)."
    - `connect-duration-ms`: "Wall-clock time from openvpn invocation to tun0 IPv4 assigned, in milliseconds."
    
    **Usage section** (NVES-12):
    - Emphasize: "The action MUST be paired with a sibling `if: always()` disconnect step"
    - Provide minimum caller shape with:
      ```yaml
      - name: Connect NordVPN (ES)
        id: vpn
        uses: pau-vega/nordvpn-actions/actions/nordvpn-es@<40-char-SHA> # vX.Y.Z
        with:
          username: ${{ secrets.NORDVPN_SERVICE_USERNAME }}
          password: ${{ secrets.NORDVPN_SERVICE_PASSWORD }}
      - name: Disconnect VPN
        if: always()
        uses: pau-vega/nordvpn-actions/actions/nordvpn-es/disconnect@<40-char-SHA> # vX.Y.Z
      ```
    - Replace all Tutellus `uses:` lines with `pau-vega/nordvpn-actions/actions/nordvpn-es@...`
    - Add notes: `environment: Preview` required, forks skip with `::notice::` (fork-safety), never `pull_request_target`
    
    **Internal Steps section** (NVES-12):
    - List 3 steps: Install → Connect (bounded retry) → Verify
    - Briefly describe what each step does
    
    **Credential Rotation section** (NVES-12):
    - Step-by-step: Generate new service credentials from NordVPN dashboard → Update `Preview` environment secrets → Verify in next workflow run → Revoke old credentials
    - Link to NordVPN dashboard: `https://my.nordaccount.com/`
    
    **Versioning section** (NVES-12, NVES-13):
    - Three pin forms table, ordered strongest to weakest:
      1. Commit SHA: `@a1b2c3...` (40 chars) — "Recommended for release-critical workflows"
      2. Exact version tag: `@nordvpn-es-vX.Y.Z` — "Frozen version from release-please"
      3. Floating major tag: `@nordvpn-es-v1` — "Auto-updates on every v1.x.y release; force-moved by CI"
    - **Explicitly state: "Never use `@main` — it is not a recommended pin form."** (NVES-13)
    - Document floating-major mutability: "The `nordvpn-es-v1` tag is force-moved on every release. For reproducibility, pin SHA."
    - Mention automated releases via release-please (Phase 5)
    
    **Troubleshooting section** (NVES-12, D-15 error messages):
    - `AUTH_FAILED` in openvpn log → "Service credentials copy-pasted with whitespace, or saved at repo level instead of `Preview` environment"
    - "Skipping e2e: VPN-gated" → "PR is from a fork (intentional fork-safety) or secrets missing from `Preview` environment"
    - Country mismatch (`country != ES`) → "Check `asn`/`exit-ip` in diagnostics, re-run PR"
    - Include exact error strings from D-15: `::error::Ubuntu runner required`, `::error::tun0 did not come up within 30s`, `::error::country mismatch`, `::error::DNS query did NOT go through tun0`
    
    **Repo references update** (D-03):
    - Replace ALL `Tutellus/tutellus-frontend-utils` → `pau-vega/nordvpn-actions`
    - Replace all `uses: Tutellus/...` → `uses: pau-vega/nordvpn-actions/...`
    - Update any links to point to correct repo
  </action>
  <verify>
    <automated>grep -c "Tutellus/tutellus-frontend-utils" actions/nordvpn-es/README.md | grep -q "^0$" && echo "No Tutellus refs" || echo "FOUND Tutellus refs - need to fix"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "Tutellus/tutellus-frontend-utils" actions/nordvpn-es/README.md` returns 0 (D-03: all repo refs updated)
    - `grep -c "pau-vega/nordvpn-actions" actions/nordvpn-es/README.md` returns at least 3 (new repo refs present)
    - `grep -c "## Usage" actions/nordvpn-es/README.md` returns 1 (NVES-12)
    - `grep -c "if: always()" actions/nordvpn-es/README.md` returns at least 1 (NVES-12: mandatory disconnect)
    - `grep -c "nordvpn-es/disconnect" actions/nordvpn-es/README.md` returns at least 1 (NVES-12: disconnect step)
    - `grep -c "## Versioning" actions/nordvpn-es/README.md` returns 1 (NVES-12)
    - `grep -c "SHA" actions/nordvpn-es/README.md | head -1` returns at least 1 (NVES-12: three pin forms)
    - `grep -c "floating major" actions/nordvpn-es/README.md` returns at least 1 (NVES-13: mutability documented)
    - `grep -c "@main" actions/nordvpn-es/README.md` returns 0 (NVES-13: NEVER recommend @main)
    - `grep -c "## Troubleshooting" actions/nordvpn-es/README.md` returns 1 (NVES-12)
    - `grep -c "AUTH_FAILED" actions/nordvpn-es/README.md` returns at least 1 (D-15 error messages)
    - `grep -c "country mismatch" actions/nordvpn-es/README.md` returns at least 1 (D-15 error messages)
    - `grep -c "Credential Rotation" actions/nordvpn-es/README.md` returns 1 (NVES-12)
    - `grep -c "NORDVPN_SERVICE_USERNAME" actions/nordvpn-es/README.md` returns at least 1 (NVES-12)
    - `grep -c "exit-ip" actions/nordvpn-es/README.md` returns at least 1 (NVES-02: six outputs documented)
    - `grep -c "connect-duration-ms" actions/nordvpn-es/README.md` returns at least 1 (NVES-02: sixth output)
    - README has valid Markdown structure (starts with `# `, has `## ` sections)
  </acceptance_criteria>
  <done>README.md ported with all repo refs updated to pau-vega/nordvpn-actions, documents Inputs/Outputs/Usage (with if: always() disconnect)/Versioning (3 pin forms, SHA first, no @main)/Credential Rotation/Troubleshooting, all D-15 error messages included</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| README → Consumer | Documentation trust: consumers follow pin-form guidance |
| README → Preview environment | Secret scoping: NORDVPN_* secrets live in Preview env only |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-17 | Spoofing | N/A | accept | Documentation only; no auth handled |
| T-02-18 | Tampering | N/A | accept | Documentation only |
| T-02-19 | Repudiation | N/A | accept | GitHub provides audit log |
| T-02-20 | Information Disclosure | `@main` recommendation | mitigate | NVES-13: README never recommends `@main`; documents its dangers explicitly |
| T-02-21 | Denial of Service | N/A | accept | Documentation only |
| T-02-22 | Elevation of Privilege | N/A | accept | Documentation only |
| T-02-23 | Secret Leak Guidance | Documents `NORDVPN_SERVICE_PASSWORD` in plaintext | mitigate | NVES-12: Clearly states secrets live in `Preview` environment, not repo-level; service credentials only (not account email/pass) |
</threat_model>

<verification>
- [ ] No `Tutellus/tutellus-frontend-utils` references remain (D-03)
- [ ] All six outputs documented (NVES-02, NVES-12)
- [ ] Three pin forms documented, SHA first, `@main` never recommended (NVES-13)
- [ ] `if: always()` disconnect step shown in Usage example (NVES-12)
- [ ] Credential Rotation section with Preview environment steps (NVES-12)
- [ ] Troubleshooting with D-15 error messages (NVES-12)
- [ ] Valid Markdown: `## ` sections present, code blocks fenced
</verification>

<success_criteria>
- `actions/nordvpn-es/README.md` exists with all repo refs updated to `pau-vega/nordvpn-actions`
- Documents: Inputs (2), Outputs (6), Usage (with `if: always()` disconnect), Versioning (3 pin forms, SHA first, no @main), Credential Rotation (Preview env), Troubleshooting (AUTH_FAILED, country mismatch, fork skip)
- All D-15 error messages included (`::error::Ubuntu runner required`, `::error::tun0 did not come up within 30s`, `::error::country mismatch`, etc.)
- No `@main` recommendation, floating-major mutability explicitly documented (NVES-13)
- Valid Markdown structure with fenced code blocks for YAML examples
</success_criteria>

<output>
After completion, create `.planning/phases/02-port-nordvpn-es/02-03-SUMMARY.md`
</output>
