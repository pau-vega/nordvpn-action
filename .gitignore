# macOS Finder metadata
.DS_Store

# Editor / IDE
*.swp
*.swo
*~
.idea/
.vscode/
*.code-workspace

# Backup and temp files
*.bak
*.tmp
*.orig

# Local-only test artifacts (the action itself writes auth files under
# $RUNNER_TEMP on a runner — these never live in the repo).
*.local
